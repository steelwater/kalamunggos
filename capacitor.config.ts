import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.steelwater.kalamunggos",
  appName: "Kalamunggos",
  webDir: "dist",
};

export default config;
